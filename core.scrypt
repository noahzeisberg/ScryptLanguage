// Lines with double slashes are comments. They'll be ignored by the interpreter.
out Welcome to the core.scrypt demonstration file.

// You can use the "wait" statement for timeouts.
wait 0.25

// Colored text demonstration
out Here's how you can make ${RED} colored ${BLUE} text.

// Run python code injections demo
do import os
do os.system("title Title of window set.")
out The title has changed!